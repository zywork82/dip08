# Web server
Flask>=3.0,<4

# Load .env
python-dotenv>=1.0,<2

# OpenAI API (Responses/Chat)
openai>=1.40.0,<2

# Google Gemini SDK
google-generativeai>=0.7,<1

# Image processing for PNG/base64 normalization
Pillow>=10,<12

# (Optional) Production WSGI server — not used in dev
# gunicorn>=21,<22